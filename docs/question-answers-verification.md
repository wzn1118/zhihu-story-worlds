# 问题回答列表与逐条拖动验收

2026-09-14 发布 `question-answers-20260914`。

知乎原网页中的热榜问题链接现在打开站内回答列表，走正式 `GET /api/v1/content/question_answers`。每条回答保留完整来源 URL 和实际接口节选，可用鼠标或手机拖动，也可点击交给刘看山。列表按 `NextOffset` 连续追加、按回答 URL 去重；翻页失败保留已读回答并提供重试；从原网页热榜进入后返回恢复原页面。保留原网页和原文入口。新增“热榜与回答”页签，热榜额度失败时仍可打开原网页热榜。

原网页提取共用回答身份识别，优先保留每条回答的实际 ID，并从当前问题 URL 补足问题 ID；19/20 位数字不经过浮点转换，正文中的引用链接不会冒充当前回答。修复了读取 iframe 滚到底但实际浏览器没有到列表末尾的问题，继续加载会推进真实页面，新增回答分别标注拖动手柄。

来源新增 `question-answer-excerpt`，保存、内容哈希、导入、刘看山收件箱和阅读来源标签一致。接口不提供作者和全文，页面明确标注“接口未提供作者”以及“官方回答节选（非全文）”。公共响应可共享缓存，但每个账号的选文、收件箱和阅读记录仍存入各自目录；匿名请求 401，旧账号标签与当前会话不符 409。

实网检查成功读取 https://www.zhihu.com/question/2081163395638760993 的第 0 和 20 条起始页，共 40 条不同回答；分页位置 20、40，实际节选逐字保存。公网候选构建进行完整应用集成：使用两套隔离测试身份及当天实际返回的 40 条响应，在桌面/手机中从克隆热榜点击、分页、真实鼠标/触摸拖入刘看山，并分别验证 40 条保存内容与账号隔离。该测试的身份和原网页热榜为受控数据，官方内容是实网响应回放；未使用真实用户 Cookie，也未冒充真实用户完成授权。

针对性服务测试 43 项通过；DOM、导航、账号和回答服务初轮 39 项通过；原网页 UI 10 场景，官方列表 UI 8 场景，列表组件桌面/手机 18 场景通过。生产 TypeScript、Vite 构建、源站部署检查通过。公网健康发布标记与文件校验单独复核。

当前 Access Secret 官方额度仅热榜 2 次/日、问题回答 10 次/日。当天热榜额度已用完，因此无法凭代码保证任意人数读取无限新问题；已读取的真实响应缓存 24 小时、在额度失败时最多保留 7 天，页面显示缓存时间。继续扩大公网使用需要提升平台额度。问题页自身的 40362 没有被解除，读取问题回答改用正式允许的开放接口。接口摘要不等同于整篇原文。
