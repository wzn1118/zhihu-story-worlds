# 重生周接入赤页 · 交付记录

2026-09-12：已将《末日的45度角躺平：重生周》的完整文字分支主线接入赤页，保留发行包内现有剧情配图、人物、道具和配乐；第一夜未接入。

## 入口与运行

- 赤页书库入口：http://127.0.0.1:4194/ ，选择《末日的45度角躺平：重生周》。已有进度时进入继续游玩。
- 源码根目录：E:\知乎；启动当前静态预览：node --import tsx scripts/redrain-preview.ts。本地预览已启动并确认页面与主线入口均返回 HTTP 200。
- 预览读取 output/redrain-build-code 的前端构建和 public 的实际资源，保留原服务器的美术私有文件拦截及动态美术路由；无需重复复制整个既有美术库。
- 标准开发入口仍为 npm run dev；正常项目打包入口 npm run build 包含 public 资源复制。本次验证使用不复制 public 的前端构建，之后由预览服务提供同一份静态资源。

## 接入内容

书库作品卡、独立作品说明、知乎原作入口、当前进度、存档导入导出、阅读设置、全屏和结局档案均已连接。主线内部的准备、分支、数值、失败回溯和结局判定沿用原版；未将其转换为 Ink 分支。

原作记录来自发行包：y甜酱不闲《末日的45度角躺平》，https://www.zhihu.com/question/540354406/answer/2588140006 。本次没有重新抓取原文；作品页明确标记互动改编。

静态主线包含 317 个源文件，共 541,002,798 字节；213 个已绑定配图文件和 50 组实际决策背景/道具引用均存在。内容计数为 5 页序幕、50 个决策位置、148 个场景变体、444 个选项、20 个常规结局、8 个失败结局。计数不等同于全路径 UI 测试。清单见 redrain-package-inventory.json；其中的 absentSourceReferences 是原包遗留但当前选择器不使用的过时引用。

## 存档与迁移

嵌入模式由赤页保存进度，键为 redleaf.redrain.v1；原版两项 doomsday-45-degree 存储与其他赤页故事存档保持独立。父子页面校验同源、iframe 来源、会话与消息格式；路线重放校验存档，确认保存成功后才退出。刷新前同步保留当前阅读位置；已收集结局在导入或回溯后继续保留。

旧版迁移：在原来游玩的浏览器、主机名和实际端口打开 /public/migrate-redleaf.html，导出后进入赤页“我的存档 → 导入重生周存档”。Windows/macOS 包的 public 目录都增加了同一份只读导出页。请使用原来的实际游玩端口；重新双击启动器可能另开端口，无法读取旧端口的浏览器存储。本次没有读取或改写用户实际旧存档。

导出按钮的 JSON 内容、备份文本和文件导入往返均通过验收。当前自动化 Chrome 在空白页也无法稳定完成下载文件落盘，因此不将浏览器文件下载记为通过。已提供“复制存档备份”：显示完整可选中的 JSON，可复制并保存为 .json 后导入。保存失败提示中同样提供最新内存进度的文本备份。

## 当前验证

- TypeScript 无错误；前端构建成功，仍有既有主包超过 500 kB 的体积提示。
- 45 / 45 定点测试通过：主线状态与真实路线重放、失败判定、存档边界、原书库合并和原存档导入。记录：../output/playwright/redrain-integrated/tests.tap。
- 桌面 1440×1000 与手机 390×844：实际开始、阅读、选择、保存、导出内容、返回、继续、刷新恢复、设置暂停/恢复；这些场景零页面错误和 HTTP 错误。
- 导入文件后实际进入 BE01、失败重试、赤页结局归档通过；以合法前序路线作为测试起点，再通过实际末次选择进入 E01 并保存。
- 注入浏览器存储写入失败：旧磁盘进度保留、最新选择可备份、失败时不退出、恢复写入后继续保存/返回、原结局保留，全部通过。
- 备份文本可完整选中，经文件选择器重新导入后路线一致；未操作系统剪贴板。
- 手机端发现并修复作品弹窗按钮被陪伴气泡遮挡。手机侧栏收起时，通过书库作品卡继续游玩。

浏览器证据位于 ../output/playwright/redrain-integrated/ ：verification.json（桌面通过项；早期手机脚本误选隐藏侧栏的失败记录已由单独手机验收补足）、verification-mobile.json、resilience.json、backup-verification.json，以及 desktop-playing.png / mobile-playing.png / failure.png / normal-ending.png / save-backup.png。下载环境诊断单独记录在 download-* 文件中。

本次是本地接入与运行验收，未发布公网；未宣称穷举所有 444 个选择或 28 个结局的浏览器路径。

## 最终手机配图补查

等待图片解码后，D01 背景为原包车内场景，实际尺寸 1672×941，fallback=false；三个选项均启用并能逐一滚动到完整可视范围，未提交选择。页面与 HTTP 错误均为 0。证据：../output/playwright/redrain-integrated/mobile-art-report.json、mobile-art-ready.png、mobile-choices-scrolled.png。随后补充手机端立绘 contain 适配。最终复核中，原图 2731×4096 在画面顶部完整显示头脚；此前选择区截图的上部隐藏来自正常滚动位置（scrollTop=206）。完整展示证据为 mobile-art-top.png 与 mobile-art-top-report.json，三个选项可逐一滚动使用，零页面和 HTTP 错误。
