# Activity UI V2 ownership

Own new src/LiukanActivityDesk.tsx/.css, optional pure helper/tests, docs/workstreams/liukan-v2-activity-ui.md. Read liukan-companion-expansion-v2.md for props. Build polished same-page modal, proper inert/focus/Escape/mobile tab handling, matched blue-white Liu Kan notebook design. Tabs: 制作中的故事 and 走过的关卡. GET /api/workshop/projects returns {projects}; WorkshopProject from shared/workshop.ts. GET /api/liukan/memories?playerId=... returns {memories}, types shared/liukan.ts. GET /api/liukan/inbox returns {posts}, only if linking saved source. No POST/paid/generation/model calls. Project search, status filter, accurate text-playable separate art approved/total/status, latest actual error, refresh and project callback. Memory search includes titles and choices; expandable actual visited scenes, no unseen data; export selected memory as actual Markdown via blob. Empty/error states honest. Do not treat ordinary progress as completed ending. Keep current player ID namespace; if absent show explicit no current player rather than fetch all players. Root wires App and pet. All needed callbacks should be optional except onClose/onProject specified contract. No service restart, browser global changes, or authored content edits.

Root verified existing backend DEFAULT_PLAYER = local-player and App companionProgress omits playerId. Root will pass playerId="local-player" to this desk, matching the existing application identity.
