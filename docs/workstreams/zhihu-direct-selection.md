# Direct selection in the authenticated Zhihu workspace

2026-09-12: user reports lossy screenshot rendering, unstable feedback and zero selectable answers despite an expanded real answer. Root owns server/zhihu-browser.ts, shared contracts, workspace integration and final live acceptance. Parallel lanes own the new inert page-document builder and its tests, the direct-selection iframe component, and a bounded contract review. Existing App/Pet/intelligence edits remain owned by the other native integration.

The page will be rendered as inert native HTML with its actual styles and media. Local text selection and scrolling do not wait for a screenshot. Controls forward validated document-scoped element IDs to the same authenticated browser. Dragging only transfers a saved server post identity; source content always comes from the browser-side capture. User session stays in its existing profile; no credentials are copied or displayed. Login and blocked pages retain real lossless screenshot handling.

Current discovery: previous4178/4179 listeners have stopped;4180 has a different active owner. Root is restoring own4178 service, preserving `.local/zhihu-browser`, and will perform its own one controlled reload for completed backend changes. No authored stories, image requests, or creative reruns are part of this correction.

14:39Z: delayed previous preview process84984 finished building and owns4178; user's persisted Zhihu login recovered successfully in the SAME profile, authenticated homepage200. This turn's attempted preview80716 exited on port conflict and owns nothing. To inspect real DOM and load the corrected renderer, root will release only4178's Zhihu service, reuse the same profile sequentially in a bounded diagnostic service, and hand back after preserving it; no cookies are read/copied, no other browser is closed.

2026-09-13 continuation: current shared4173/PID12896 owns the app's persisted Zhihu browser, with the old screenshot API. Root keeps that server alive. The scoped4178 dev server will own the browser after a graceful browser-only /close on4173, preserving its profile. Removed the temporary DOM logging monkeypatch. Passive native-frame reads now preserve control IDs; dragging saves the displayed server snapshot even after unrelated remote page changes. Capture still requires a registered frame/post pair and exact stored source.

QA inventory: real authenticated page in the workshop; native typography and source images at desktop/DPR2; mouse text selection and drag to Liu; pointer handle drag and cancelled-drag recovery; touch feed at390; local scroll with no API request; passive simultaneous frame reads; expand-answer response; exact persisted source/title/author/link; workshop return/reopen in one page. Off-path checks: unknown/stale frame identity and canceled drag followed by a click. Test fixtures are recorded separately from real webpage acceptance.
06:43 China: pending ReadingDesk landed. Current tsc reports only LiukanReadingDesk.tsx:234/266 string|undefined passed to string; reading_ui retains those fixes. Our modified browser/UI files have no reported type error.

## Verified continuation / 2026-09-13

Current reviewed source changes:
- Native frame GETs share the displayed document and leave live control IDs intact. Login/empty-shell frames can refresh; the UI refreshes empty loading content at most three times, then retains its explicit reload control.
- Source capture requires a registered frame/post identity and saves the exact trusted server snapshot. Expanding another answer or a recommendation change no longer invalidates a source already on screen.
- Native text selection and the post handle both support pointer-captured dragging across the iframe edge. A canceled drag does not swallow the next ordinary click. Plain paragraph clicks do not forward focusable article wrappers as website account actions.
- Local controls attach at DOM readiness without waiting for every remote image. Late iframe load events do not reset an active interaction.
- Native paragraph transport preserves inline mentions authored by React; an invalid p/div nesting previously caused the HTML parser to truncate the RichText wrapper. The targeted conversion retains computed display and all text.
- Reading position and selected-answer identity survive workspace unmount/reopen in a bounded in-memory URL map. Native scrolling sends no browser API request. Mobile keeps the original article typography in a fitted column and hides the non-reading composer and desktop sidebar.

Real desktop acceptance: output/playwright/zhihu-smooth/verification.json. Existing app-owned authenticated profile; actual homepage exposed12 answers. An expanded Cat Chen answer,891 characters, was compared byte-for-byte with the saved inbox source. Pointer-handle drag201, directly selected native text drag201, canceled drag followed by click201, simultaneous passive reads retain document ID, and zero browser requests during local scrolling. Closing/reopening the workshop restores scrollY1433. Reopening an already prepared page took457ms in this run. Full scripted drag including deliberate movement and a screenshot took3780ms; this is not a pure network benchmark.

Real mobile acceptance: output/playwright/zhihu-smooth/mobile-verification.json,390x844 at DPR3, one independent context/page. Touch drag201 and source saved; latest drop-to-save2184ms while build/QA were running, earlier verified run445ms. Do not promise a fixed latency from those samples. Native content fits iframe width with no horizontal document overflow. User tabs were not created or closed. Full-DPI image: mobile-native-3x.png; CSS-scale screenshots: desktop.png,desktop-selection.png,desktop-dragging.png,desktop-fed.png,desktop-reopened.png,mobile.png,mobile-dragging.png,mobile-fed.png. Screenshots were visually inspected, including mobile reading fit and drag feedback.

TypeScript passed before subsequent concurrent generation edits. The final whole-tree recheck currently reports the other lane's missing workshop-simple-images.ts plus workshop-worker.ts:50/51 FastAdaptationMode/FastStoryProgress mismatches; this lane preserves those active edits and does not claim a final whole-tree type pass. Production frontend build passes (copyPublicDir=false, existing public resources retained); Vite retains its large-chunk advisory. Focused current tests are recorded separately below. Earlier failure.json files document real defects and test-helper corrections; the final verification.json/mobile-verification.json are the accepted runs. Test fixtures are not real answers or generated illustrations. This turn did not regenerate a story or request scene art.

Runtime: dedicated4178/PID61884 serves current source, owns the preserved Zhihu profile, and remains running for the user. Shared4173/PID12896 remains running with its older compiled UI/backend. Do not claim the native-document changes are loaded on4173. Root reading-skill integration may coordinate a later shared refresh; do not start another browser against the same profile concurrently.

Final focused validation:18/18 tests passed (browser9, native-document5, actual DOM extraction fixture1, drop parser3). Final whole-tree TypeScript recheck: workshop-simple-images.ts now exists; remaining concurrent errors are workshop-worker.ts:50/51 (FastAdaptationMode / FastStoryProgress) and workshop-simple-images.test.ts relay fixtures missing usage/responseId. Preserve those workers' edits. The accepted frontend build and real desktop/mobile interaction evidence remain separate from that pending whole-tree integration.
