# Root bounded review
Please reading_backend consider before final: (1) get validates only top level; corrupt saved sections/evidence can crash UI or expose unvalidated quotes. Validate persisted shape/evidence source IDs/lengths and historical embedded quotes safely; current=false for stale sources rather than accepting corrupt payload. (2) preserve received model answer atomically in private attempts before validation, otherwise failed output is lost and there is no repairable diagnostic. Do not expose raw body or change provider settings. (3) request mapping mkdir then write can race same requestId; same-process same request may incorrectly409 if mapping file pending. Test simultaneous exact requestIds. (4) full single-source14k budget drops middle for long texts; reuse postReadingContext question-specific relevance to retain important clue near middle if practical, mark complete false; quotes must exactly match original supplied passage. (5) respond honest busy/error and avoid duplicate requests; no new paid retries.
