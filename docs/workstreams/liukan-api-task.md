Read E:/CodexHome/skills/zhihu/SKILL.md and all relevant references including CLI, HTTP, user-api, OAuth, hackathon, MCP. Implement validated server-side read-only ability dispatch and persistent configurable relay for pet general chat and inbox/progress responses, using existing transport module if practical. Never print/store keys in browser storage. Expose sanitized GET/POST config and real tested request. Only modify server/liukan modules + new shared capability types + independent frontend settings component. Keep arbitrary shell commands unavailable. Root mounts router and panel. Existing inbox needs /chat vs frontend /ask mismatch repair. Use configured default Zhihu CLI or selected relay truthfully, no simulated model answers. Report docs/workstreams/liukan-capabilities.md.

ROOT UPDATE: Actual pet /chat path already fixed. shared source union expanded. Repeated type/build checks currently blocked by syntax at server/liukan/answer.ts:20:306 expected colon; please fix your in-progress file first to allow root preview start. Root awaits final exact settings component/router API. Current ports4178 &4179 other processes; root owns forthcoming4180 static preview.
