Implement ONLY the P2 ending memory growth issue in docs/workstreams/liukan-final-review.md. Own server/liukan/memory.ts, focused memory tests, add report section. Verified same story/version/ending memory should grow when player reads more paragraphs along same path, never replace richer data with shorter snapshot, preserve50 endings and game history validation. Do not merge different routes blindly. Read existing services before choosing. No App/Pet/shared UI edits or servers/browser/real API calls. Root fixes frontend review items locally. Run focused tests then report. Do not commit.