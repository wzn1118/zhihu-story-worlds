# Reading UI lane
Own ONLY new src/LiukanReadingDesk.tsx + .css and own report. Read docs/workstreams/liukan-reading-api.md and shared/liukan-reading.ts (published) and frontend-design skill. User wants more useful capabilities, high quality UI, same page. Export component LiukanReadingDesk with props { initialPostId?: string; onClose:()=>void; onReadPost?:(post:LiukanInboxPost)=>void; onProject?:(project:WorkshopProject)=>void }. Root mounts standalone overlay from pet/capability buttons; YOU handle overlay positioning/styles, Escape/focus trap/inert and body scroll, but no App edits. Use current actual supplied asset in public/assets/liukan (no images generated). UI: elegant blue-white notebook editorial look, desktop broad workspace with source list/10 skill cards/completed note; mobile readable stacked/tabs. GET /api/liukan/inbox and /api/liukan/reading on open (no model automatically). Search saved sources by title/author/text locally, choose1 or2-3 for compare, optional focus question, explicit run button with real pending/error. Retain requestId for retries same exact payload to prevent duplicate model calls. Show distinct draft/adaptation labels, evidence quotes and corresponding saved source reader (via onReadPost). GET notes durable history and detail; export completed note via copyable Markdown and local browser download. Reopening retains notes. Choose another source clears result; late responses must not attach wrong selection. onProject optional: existing source has projectId? show truthful project status/fetch existing only; you may omit generation here to keep clear scope. Add no new external API requests, no paid generation, no servers or browsers. Scope10 actual model reading tasks per contract. Natural Chinese copy without developer chatter, no fake success. Coordinate with reading_backend on shared contract changes. Root performs live checks after integration. Finish tsc and report.

Root integration tsc currently only sourceScope param accepts string while origin.contentScope may undefined at lines234/266; please fix optional string. Please also check source preview focus: while preview open, Tab should stay in preview rather than underlying desk, Escape should close preview first. API mounted and isolated4180 ready after backend refresh, root is starting actual browser QA.

Independent review P2: compare display order follows inbox order, input postIds/backend canonicalized by ID, so first/second references mismatch. Please sort selectedPosts by ID to match canonical input and visibly number1/2/3 in selected chips. Root needs this fixed before comparison live test.
