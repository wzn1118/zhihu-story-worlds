# Catalog B 分支因果复核

复核日期：2026-09-06。修改范围为 `content/catalog-b.ts` 的八个世界及本文档。此次逐篇读取 `.local/zhihu-cache/story-<storyId>.json` 的公开 API 节选，并复查 `docs/catalog-b-source-review.md`。每篇缓存仍为 3000 字符，游戏原创的分支后果没有回写原文，也没有新增未读到的原作结论。

## 共用流程

后六个世界共有 24 轮调查。原来的成功、误判和暂缓都会进入同一个 `aftermath_i`，而正文常直接描述成功行动；`rush_i` 跳过核查后，也可能读到已经写好记录的叙述。

- 24 个共用后续场景全部改为各入边都成立的现场状态和下一事件，不再统一赠送成功结果、调查记录、交接或已落实的照料。
- 每轮分别撰写成功、误判、暂缓结果，共 72 条，保存在本文件局部 `Trial.outcomes`，由既有 `Choice.feedback` 显示。误判仍消耗原有数值，暂缓仍不获得推理结论；没有新增隐藏状态。
- 24 个 `inspect_i` 和 24 个 `rush_i` 增加反馈：前者明确留下本轮证据但仍需判断，后者明确没有核查本轮证据。共用判断段不再宣称玩家已经完成事实与推测的区分。
- `answer_i` 仍要求本轮调查线索；`earned_ending` 仍要求最后两轮结论及至少 2 点储备；`partial_ending` 仍只要求任意一轮结论。结局正文因此不能再假定前面所有调查都成功。

| 世界 / 来源 ID | 本轮具体修正 |
| --- | --- |
| `six-roots` / `1760265980192886784` | 算式纸、姓名应答、护场诗答和暂停申请只在对应成功反馈成立。错答不再被后续改写成完成解法；暂缓诗答明确退开，没有化解一击。部分结局只带回已核对的部分，不再保证同时拥有解法与见证。 |
| `palace-ledger` / `1793742089336532992` | 跳过旧册不再读到核过数字；加倍服侍不再自动形成劳动边界；泛指所有经手者有罪不再自动生成正常报销方案。第四轮不再假定手中已有两份完整记录；部分结局不再保证资助凭证一定取得。 |
| `red-plum` / `1930445234262750503` | 门板配给、双方隔开与伤者照料、观察记录、外出接应分别归对应成功反馈。否认伤害不再自动获得妥善安置。独自派出女儿的错误行动需要额外菜粮找人接回；暂缓则留在灯下。第三轮题干使用假设，不预设已取得记录；部分结局承认未观察的日期空缺。 |
| `hollow-immortals` / `1558118587956662272` | 跳过丹丸、灵脉或村庄证言核查不再自动留下对应纸张。第四轮明确先前见闻可能尚未成文；成功只分层整理实际可核部分，缺少的早期记录标为待补。白鹤村死讯没有被否认选项变成存活反转。 |
| `island-broadcast` / `1783485301039054849` | 传呼机重复确认、交接条件、保持距离和清点分配只在相应成功反馈成立。制造话题或暂缓不再自动吃上晚饭。部分结局完整列出核验与空缺，不声称全部任务已完成。 |
| `wrong-realm` / `1716453753710972928` | 重核任务、药材归属、救援时间线与统一裁定各有独立结果。误判或跳过后不再共享“拒绝让步”“时间线已成”“两种记录已提交”的成功叙述。撤离结局保留未厘清的争议，不预设已提出申诉。 |

## 独立分支

| 世界 / 来源 ID | 本轮具体修正 |
| --- | --- |
| `temple-heart` / `2025954672918163637` | `promise` 不再把立即下山分支强行写成两天后；`visit` 不预设玩家选择过清粥嘱咐或有施法记录，`pulse` 明确本次少量渡气与查验。`stalled` 不凭空摊开恢复记号，三种查验或暂缓反馈区分结论范围。`release` 与 `defer` 明确解除及暂搁的区别；`sustain` 与 `ask_village` 交代实际观察或邻里承诺。`review` 不保证已经跨过门槛，`ending_boundary` 不再替未解除索偿的路线解除旧约，也不否认早先夸下过承诺。 |
| `tiger-shelter` / `1962166083206242442` | `identity` 不假设先前已表明身份；`return_path` 兼容独行后会合及君洲已经同行。`proof` 不代替玩家完成责任与处置的区分；`review` 不再对沉默路线说名字已经说过。`negotiate` 的反馈只落实陪同，`ending_terms` 将入京用途与后续交接保留为待核事项。离山结局不否认此前已经解释过的路线。 |

## 验证

- `node node_modules/typescript/bin/tsc --noEmit` 通过。
- 八个世界均由真实 `compileWorld` 编译为 Ink：128 场景、246 选项、24 结局。
- `tests/backend-worlds.test.ts` 按八个世界 ID 筛选运行，8 项通过：穷举 75,697 个规则状态，所有场景、246 条选项边及 24 个结局均由 Ink 回放，无无出口状态。每个世界的节点、选项和结局数量分别为：前两篇 16 / 30 / 3，后六篇各 16 / 31 / 3。
- `tests/choice-outcomes.test.ts` 按同样 ID 筛选运行，8 项回退与重放测试通过。
- 另用一次性 Node 断言运行 120 组实际选择组合：6 世界 × 4 轮 × `inspect→answer/error/defer` 与 `rush→error/defer`。验证证据、正确选项解锁、推理线索、实际显示的反馈与结局门槛；此前各轮暂缓时，单独答对最后一轮不能解锁完整结局。
- 另验证 3 条独立路线：未记饮食及恢复阶段且未解除索偿的 `temple-heart` 边界结局；保持沉默的 `tiger-shelter` 不解锁以真名留下；未确认真实身份但成功争取陪同的路线保留入京用途待核。
- 将修改前后的世界 ID、版本、来源、资源定义、起点、节点 ID、结局标题及全部选项（排除反馈）的结构化投影逐字比较，一致。选项文字、效果、要求和边没有改动。
- `.local/zhihu-cache` 的全部 21 个文件 SHA-256 与修改前逐项一致。没有访问凭据或生成美术。

## 集成时补正

根代理随后修正了两处选项文字：`temple-heart/collapse/admit` 改为“承认自己需要休息，接受照料”；`tiger-shelter/terms/run` 改为“消耗体力沿岩石退开，寻找下山的路”，对应场景标题改为“脚下的碎石”。数值、条件和边不变；两条精确旧文保存在 `legacyTexts`，实际旧 Ink 存档在选择前后均通过迁移测试。上文结构投影一致的结论对应子任务完成时的版本，不包含这两处后续文字修正。

## 保留的问题

- 后六篇仍共享四轮结构，成功、误判、暂缓仍汇合到同一节点。本轮提供各自反馈和不矛盾的公共叙述，没有新增长期分支或独立玩法。
- 本轮未作浏览器视觉检查。自动验证覆盖图、状态、反馈传递和所列路线；叙事语义由逐段人工复核，不能据此宣称所有文学或节奏问题已经解决。
